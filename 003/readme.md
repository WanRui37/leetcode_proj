# 最长连续序列

给定一个未排序的整数数组 nums ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。

请你设计并实现时间复杂度为 O(n) 的算法解决此问题。

## 方法一
- 哈希表: 使用unordered_set存储所有数字
- 具体实现: 
    - 将所有数字存入哈希集合中
    - 遍历集合中的每个数字，只从序列的起点开始计数（即当前数字-1不在集合中）
    - 对于每个起点，向后查找连续的数字，统计连续序列长度
    - 更新最大连续序列长度
- 时间复杂度：O(n)
- 空间复杂度：O(n)